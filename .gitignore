# dependencies (bun install)
node_modules

# output
out
dist
.build

# generated content registries (built by build:articles / build:events on install;
# never commit — contributors only add the source Markdown)
src/content/registry.ts
src/content/events.registry.ts

*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
.wrangler
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Source assets (used to generate favicons etc.)
era_logo.png
